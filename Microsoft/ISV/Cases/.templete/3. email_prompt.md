# Generate `3. email.md`

Case Path: `[case folder path]`

Read: `1. Scope.md`, `2. after meeting.md`, `3. email.md`, `99. ISV_templete/README.md`, `99. ISV_templete/*.json`, `reference/`

Write `3. email.md` in English. Output email body only.

Subject: `[CASE_ID] Meeting results and follow-up resources - [Customer]`

Opening: `Hello [name],` then meeting topics, conclusion, and referenced services in prose.

Body: one `##` section per service. Use exact service names only. Do not merge services.

Under each `## Service` heading, write service-level explanation first: what the service is, what function is needed, and why it is needed for this meeting/project.

Add a table when the service has options, settings, scope, ownership, or decision items to compare. Put the table before feature sections.

Feature sections: use `### 1. [Feature name]`, `### 2. [Feature name]`, `### 3. [Feature name]`, etc.

Under each feature, write numbered click-path steps only. Use `>` to show what to click. Put input fields, values, options, and checks as bullets under the same step. Do not write explanatory sentences under the feature section.

End each service with `**Reference:**` and include `Link`, `Best practice`, and `Diagram` items when available.

Official Microsoft domains only: `learn.microsoft.com`, `azure.microsoft.com`, `partner.microsoft.com`, `marketplace.microsoft.com`, `github.com/Azure`, `github.com/microsoft`.

`## Next checkpoints`: numbered list, meeting-specific items only, no owner labels.

Closing: `Best regards,` + Kim + email from source files.

Do not: use owner-style labels, broad platform labels, abstract recommendations, hardcoded IDs or names, invented facts, secrets, CRM URLs, or prompt explanations.
