Subject: [ME-127199] Meeting results and follow-up resources - Ailytics

Hello Lenard,

Following our architecture review session, I captured the meeting results and follow-up resources by service area. We discussed ELISA's video analytics workflow, Kubernetes-based architecture, SaaS and on-premise deployment models, AWS-to-Azure migration scope, Redis cost optimization, future multi-tenant support with Microsoft Entra ID, and the upcoming Marketplace publication discussion. The main conclusion is that the Azure migration scope is focused on the web frontend and backend services, while AI inference remains on-premise for most customers due to bandwidth and data sensitivity requirements.

Services referenced during the meeting: Azure Kubernetes Service, Azure Blob Storage, Azure Functions, Azure Database for PostgreSQL, Azure Service Bus, Azure Cache for Redis, Microsoft Entra ID, and Microsoft Commercial Marketplace / Partner Center.

## Azure Cache for Redis

Azure Cache for Redis is needed to map the current Redis-compatible workload to an Azure option during the AWS-to-Azure migration. The required function is Redis-compatible caching for the ELISA application path, and the meeting-specific need is to review cost and operational trade-offs as camera scale grows from approximately 4,000 cameras toward 7,000 cameras.

| Area | Meeting-specific item | Decision item |
|---|---|---|
| Current usage | Redis-compatible service in the existing platform | Confirm the exact Redis workload pattern |
| Managed option | Azure-managed Redis-compatible cache | Decide whether to evaluate Azure Cache for Redis first |
| Self-managed option | Redis-compatible deployment in containers or VMs | Decide whether cost savings justify operational ownership |

### 1. Managed Redis-compatible cache
1. Azure portal > `Create a resource` > `Databases` > `Azure Cache for Redis` > `Create`
	- `Subscription`: target Azure subscription
	- `Resource group`: target resource group
	- `DNS name`: ELISA Redis cache name
	- `Region`: target Azure region
	- `Cache type`: Azure Cache for Redis tier
	- `Cache size`: size for the test workload
2. `Networking`
	- `Connectivity method`: `Private endpoint` or `Public endpoint`
	- `Virtual network`: target ELISA virtual network
	- `Subnet`: private endpoint subnet, if private endpoint is used
3. `Advanced`
	- `TLS`: enabled
	- `Non-TLS port`: disabled unless required
	- `Clustering`: enable only when required
	- `Data persistence`: enable only when required
4. `Review + create` > `Create`
5. Azure Cache for Redis resource > `Access keys` or `Authentication`
	- Copy endpoint
	- Copy port
	- Copy access key or authentication configuration
	- Add values to ELISA backend Redis configuration
6. Azure Cache for Redis resource > `Monitoring` > `Metrics`
	- Check `Connected Clients`
	- Check `Cache Read`
	- Check `Cache Write`
	- Check `Server Load`
	- Check latency during test traffic

### 2. Self-managed Redis-compatible option
1. AKS deployment path > Kubernetes manifest
	- Runtime: `Redis OSS` or `Valkey`
	- Objects: `StatefulSet`, `Service`, `PersistentVolumeClaim`, `ConfigMap`, `Secret`
2. `StatefulSet` manifest
	- `resources.requests.cpu`: test value
	- `resources.requests.memory`: test value
	- `resources.limits.cpu`: test value
	- `resources.limits.memory`: test value
	- `readinessProbe`: Redis ping check
	- `livenessProbe`: Redis ping check
	- `volumeMounts`: Redis data path
3. Azure portal > `Virtual machines` > `Create` > `Azure virtual machine`, if VM option is tested
	- `Image`: Linux distribution
	- `Disk`: managed disk for Redis data
	- `Networking`: private subnet
4. ELISA backend configuration
	- `Redis endpoint`: AKS service DNS name or VM private IP
	- `Port`: Redis-compatible port
	- `Password/secret`: Kubernetes secret or VM secret store value
	- `TLS`: enabled or disabled per test design
5. Azure portal > `Monitor` or AKS > `Insights`
	- Check CPU
	- Check memory
	- Check disk
	- Check connection count
	- Check restart count
	- Check availability
6. Cost comparison worksheet
	- Monthly infrastructure cost
	- Failover steps
	- Patching owner
	- Backup owner
	- Recovery time

**Reference:**
- Link: Azure Cache for Redis overview: https://learn.microsoft.com/azure/azure-cache-for-redis/cache-overview
- Best practice: Azure Cache for Redis best practices: https://learn.microsoft.com/azure/azure-cache-for-redis/cache-best-practices
- Diagram: AKS baseline architecture with Azure services: https://learn.microsoft.com/azure/architecture/reference-architectures/containers/aks/baseline-aks

## Microsoft Entra ID

Microsoft Entra ID is needed for future authentication and tenant-aware access design. The required function is multitenant identity integration, and the meeting-specific need is to define how customer tenant IDs will be registered and how ELISA should separate access across customers, projects, sites, or deployments.

| Area | Meeting-specific item | Decision item |
|---|---|---|
| Current state | Single domain for all clients | No tenant synchronization needed now |
| Future state | Tenant ID registration and Microsoft Entra ID integration | Confirm authentication-only or authorization/admin model |
| Tenant boundary | Customer, project, deployment, or site boundary not finalized | Confirm tenant boundary model |

### 1. Multitenant application registration
1. Azure portal > `Microsoft Entra ID` > `App registrations` > `New registration`
	- `Name`: ELISA application registration name
	- `Supported account types`: `Accounts in any organizational directory (Any Microsoft Entra ID tenant - Multitenant)`
	- `Redirect URI`: `Web`
	- `Redirect URI value`: ELISA sign-in callback URL
2. `Register`
3. App registration > `Authentication`
	- Add required redirect URIs
	- Add logout URL if used
4. App registration > `API permissions` > `Add a permission`
	- API: Microsoft Graph or application API
	- Permission type: delegated or application
	- Permission names: required sign-in/API permissions
5. `Grant admin consent`, if required
6. App registration > `Certificates & secrets`
	- Secret or certificate: create client secret or upload certificate
	- Store value in ELISA identity configuration
7. ELISA sign-in test
	- Account: external customer tenant account
	- Check: sign-in succeeds and tenant ID is captured

### 2. Tenant boundary model
1. ELISA configuration model > tenant boundary field
	- Boundary value: `customer`, `project`, `deployment`, `site`, or approved model
	- Field: `tenantId`
2. ELISA data model > boundary mapping
	- Map `users` to boundary key
	- Map `cameras` to boundary key
	- Map `sites` to boundary key
	- Map `alerts` to boundary key
	- Map `metadata` to boundary key
	- Map `access roles` to boundary key
3. ELISA authorization layer > query filter
	- Filter value: authenticated user's tenant boundary
	- Scope: cameras, alerts, metadata, sites
4. ELISA test data
	- Tenant A records
	- Tenant B records
5. ELISA sign-in test
	- User: tenant A user
	- Check: tenant B records are not returned

**Reference:**
- Link: Microsoft Entra ID multitenant application registration: https://learn.microsoft.com/entra/identity-platform/howto-convert-app-to-be-multi-tenant
- Best practice: Azure multitenant solution architecture guidance: https://learn.microsoft.com/azure/architecture/guide/multitenant/overview
- Diagram: Multitenant SaaS architecture guidance: https://learn.microsoft.com/azure/architecture/guide/multitenant/overview

## Azure Kubernetes Service

Azure Kubernetes Service is needed as the Azure managed Kubernetes platform for ELISA's containerized backend and web workloads. The required function is Kubernetes workload hosting, and the meeting-specific need is to define which frontend and backend services move to Azure first while AI inference remains on-premise for most customers.

| Area | Meeting-specific item | Decision item |
|---|---|---|
| Current architecture | Most backend services and AI connections run in Kubernetes | Confirm workload scope for first Azure phase |
| Scale | About 200 active deployments across 4,000 cameras | Confirm production baseline requirements |
| Growth | Expected growth toward 7,000 cameras | Confirm autoscaling and monitoring requirements |

### 1. Kubernetes workload hosting
1. Azure portal > `Kubernetes services` > `Create` > `Create a Kubernetes cluster`
	- `Subscription`: target Azure subscription
	- `Resource group`: target resource group
	- `Cluster name`: ELISA AKS cluster name
	- `Region`: target Azure region
	- `Kubernetes version`: selected supported version
2. `Node pools`
	- `frontend`: node pool name, VM size, node count
	- `backend`: node pool name, VM size, node count
	- `processing`: node pool name, VM size, node count, if required
3. `Networking`
	- `Network configuration`: Azure CNI
	- `Virtual network`: target ELISA virtual network
	- `Subnet`: AKS subnet
	- `Service CIDR`: selected service address range
	- `DNS service IP`: selected DNS IP
	- `Network policy`: selected policy option
4. `Integrations`
	- Azure Monitor: enabled
	- Container Insights: enabled
5. `Review + create` > `Create`
6. AKS cluster > `Namespaces` or deployment manifest
	- Namespaces: frontend, backend, processing
7. Deployment pipeline or `kubectl apply`
	- Images: ELISA frontend and backend container images
	- Manifests: Deployment, Service, ConfigMap, Secret
8. AKS cluster > Ingress controller configuration
	- Route: web frontend
	- Route: API endpoint
	- Check: service health endpoint returns success

### 2. Production baseline architecture
1. AKS cluster > `Node pools`
	- System node pool: system workloads only
	- User node pools: ELISA workloads
2. User node pool > `Scale method`
	- Autoscaling: enabled
	- Minimum nodes: agreed baseline
	- Maximum nodes: agreed growth limit
3. Kubernetes manifest > container resources
	- `resources.requests.cpu`: service baseline
	- `resources.requests.memory`: service baseline
	- `resources.limits.cpu`: service limit
	- `resources.limits.memory`: service limit
4. Kubernetes manifest > probes
	- `readinessProbe`: service readiness endpoint
	- `livenessProbe`: service health endpoint
5. AKS cluster > `Workload identity`
	- Managed identity: ELISA workload identity
	- Role assignment: required Azure service roles
6. AKS cluster > `Insights`
	- Check pod CPU
	- Check pod memory
	- Check restart count
	- Check node utilization

**Reference:**
- Link: AKS overview: https://learn.microsoft.com/azure/aks/intro-kubernetes
- Best practice: AKS production baseline architecture: https://learn.microsoft.com/azure/architecture/reference-architectures/containers/aks/baseline-aks
- Diagram: AKS architecture design guidance: https://learn.microsoft.com/azure/architecture/guide/aks/aks-start-here

## Azure Blob Storage

Azure Blob Storage is needed for alert snapshots, before/after video clips, and cloud-hosted media storage. The required function is object storage with lifecycle management, and the meeting-specific need is to support client-dependent retention policies such as immediate deletion, six-month retention, or one-to-two-year retention.

| Area | Meeting-specific item | Decision item |
|---|---|---|
| Media type | Snapshots and before/after video clips | Confirm media types to migrate first |
| Retention | Client-dependent retention | Confirm standard and exception policies |
| Deletion | Immediate, six-month, or one-to-two-year retention | Map policies to lifecycle rules |

### 1. Media storage
1. Azure portal > `Storage accounts` > `Create`
	- `Subscription`: target Azure subscription
	- `Resource group`: target resource group
	- `Storage account name`: ELISA storage account name
	- `Region`: target Azure region
	- `Performance`: Standard or Premium
	- `Redundancy`: selected redundancy option
2. `Networking`
	- Access method: restricted public access or private endpoint
	- Virtual network: target ELISA virtual network, if private endpoint is used
	- Subnet: private endpoint subnet, if private endpoint is used
3. `Review` > `Create`
4. Storage account > `Data storage` > `Containers` > `+ Container`
	- Container: `alert-snapshots`
	- Container: `video-clips`
5. Storage account > `Access Control (IAM)` > `Add role assignment`
	- Role: required Blob Data role
	- Member: ELISA workload identity
6. ELISA media upload configuration
	- Storage account name
	- Container names
	- Authentication method
7. Storage account > target container > `Upload`
	- Test file: snapshot
	- Test file: video clip
	- Check: read, delete, and access control behavior

### 2. Lifecycle management
1. Storage account > `Data management` > `Lifecycle management` > `Add a rule`
	- `Rule name`: `standard-six-month-delete`
	- `Rule scope`: target container or blob prefix
2. `Base blobs`
	- Action: delete
	- Condition: last modified more than 180 days ago
3. `Add a rule`
	- Rule: immediate deletion exception, if required
	- Rule: one-year or two-year retention exception, if required
4. Blob index tags or container prefixes
	- Tag/prefix: standard retention
	- Tag/prefix: immediate deletion
	- Tag/prefix: extended retention
5. Target container > `Upload`
	- Sample blobs: matching tags/prefixes
	- Check: lifecycle rule scope before production enablement
6. Database deletion job configuration
	- Retention class: same as media blob retention
	- Project completion date: retention start field

**Reference:**
- Link: Azure Blob Storage overview: https://learn.microsoft.com/azure/storage/blobs/storage-blobs-overview
- Best practice: Azure Storage security recommendations: https://learn.microsoft.com/azure/storage/blobs/security-recommendations
- Diagram: Azure Blob Storage lifecycle management: https://learn.microsoft.com/azure/storage/blobs/lifecycle-management-overview

## Azure Functions

Azure Functions is needed for serverless event processing mapped from the Lambda/Azure Functions dependency discussed in the meeting. The required function is event-driven execution for alert, metadata, or workflow processing outside the main Kubernetes services.

### 1. Serverless event processing
1. Azure portal > `Function App` > `Create`
	- `Subscription`: target Azure subscription
	- `Resource group`: target resource group
	- `Function App name`: ELISA function app name
	- `Region`: target Azure region
	- `Runtime stack`: runtime used by the function
	- `Version`: runtime version
2. `Hosting`
	- `Storage account`: target storage account
	- `Operating system`: selected OS
	- `Plan`: selected hosting plan
3. `Monitoring`
	- Application Insights: enabled
4. `Review + create` > `Create`
5. Function App > `Functions` > `Create`
	- Trigger: `HTTP trigger`, `Service Bus trigger`, `Blob trigger`, or `Timer trigger`
	- Connection setting: target connection setting name
	- Queue/container/route: target event source value
6. Deployment pipeline or Function App deployment
	- Source: function source path
	- Target: Function App
7. Function App > `Monitor`
	- Check execution result
	- Check duration
	- Check failure count
	- Check logs with a test event

**Reference:**
- Link: Azure Functions overview: https://learn.microsoft.com/azure/azure-functions/functions-overview
- Best practice: Azure Functions best practices: https://learn.microsoft.com/azure/azure-functions/functions-best-practices
- Diagram: Azure Functions hosting options: https://learn.microsoft.com/azure/azure-functions/functions-scale

## Azure Database for PostgreSQL

Azure Database for PostgreSQL is needed for the PostgreSQL dependency discussed in the architecture review. The required function is managed relational database hosting for alert records, metadata, dashboard queries, and data-heavy modules such as license plate detection.

| Area | Meeting-specific item | Decision item |
|---|---|---|
| Database dependency | PostgreSQL | Confirm migration scope and target server configuration |
| Heavy module | License plate detection with 20,000-30,000 records per day | Confirm query and indexing review scope |
| Dashboard | Metrics with multiple joins | Confirm performance baseline |

### 1. Managed PostgreSQL database
1. Azure portal > `Azure Database for PostgreSQL flexible servers` > `Create`
	- `Subscription`: target Azure subscription
	- `Resource group`: target resource group
	- `Server name`: ELISA PostgreSQL server name
	- `Region`: target Azure region
	- `PostgreSQL version`: selected version
	- `Compute tier`: selected tier
	- `Storage size`: target storage size
	- `Backup retention`: target retention days
2. `Authentication`
	- Method: Microsoft Entra authentication, PostgreSQL authentication, or both
3. `Networking`
	- Access: private access or public access
	- Virtual network/subnet: AKS-accessible network, if private access is used
	- Firewall rules: AKS access range, if public access is used
4. `Review + create` > `Create`
5. PostgreSQL flexible server > `Databases`
	- Database name: ELISA target database
6. PostgreSQL flexible server > users/roles
	- User/role: ELISA application user
	- Permissions: required schema and table permissions
7. Migration tool or approved migration method
	- Source: current PostgreSQL environment
	- Target: Azure Database for PostgreSQL
	- Scope: schema and data
8. ELISA backend configuration
	- Connection string: Azure PostgreSQL endpoint
	- Check: application connectivity

### 2. Query and data-volume review
1. PostgreSQL flexible server > `Server parameters`
	- Parameter: `shared_preload_libraries`
	- Value: include `pg_stat_statements`, if required
2. PostgreSQL flexible server > `Query Performance Insight`
	- Check top queries by duration
	- Check top queries by execution count
3. Database schema > index review
	- Columns: timestamp, camera, site, status, license plate detection fields
4. Database schema > index change
	- Add or adjust indexes after query plan check
5. Test data load
	- Data volume: production-like license plate detection record count
6. ELISA dashboard test
	- Check pagination
	- Check filters
	- Check dashboard query response
7. Retention/archive job configuration
	- Policy: aligned with media lifecycle rules

**Reference:**
- Link: Azure Database for PostgreSQL Flexible Server overview: https://learn.microsoft.com/azure/postgresql/flexible-server/overview
- Best practice: Azure Database for PostgreSQL performance best practices: https://learn.microsoft.com/azure/postgresql/flexible-server/concepts-query-performance-insight
- Diagram: Azure Database for PostgreSQL architecture guidance: https://learn.microsoft.com/azure/architecture/databases/guide/design-azure-postgresql

## Azure Service Bus

Azure Service Bus is needed for the SQS/Service Bus dependency discussed in the meeting. The required function is managed messaging and queueing for decoupled alert, workflow, and backend processing between ELISA components.

### 1. Enterprise messaging and queues
1. Azure portal > `Service Bus` > `Create`
	- `Subscription`: target Azure subscription
	- `Resource group`: target resource group
	- `Namespace name`: ELISA Service Bus namespace
	- `Location`: target Azure region
	- `Pricing tier`: Standard or Premium
2. `Review + create` > `Create`
3. Service Bus namespace > `Entities` > `Queues` or `Topics` > `+ Queue` / `+ Topic`
	- Name: alert, workflow, or backend processing entity name
	- Max size: target value
	- Lock duration: target value
	- Max delivery count: target value
	- Duplicate detection: enabled or disabled
	- Dead-letter settings: enabled
4. Service Bus namespace > `Access Control (IAM)` > `Add role assignment`
	- Role: `Azure Service Bus Data Sender` or `Azure Service Bus Data Receiver`
	- Member: ELISA workload identity
5. ELISA backend configuration
	- Namespace: Service Bus namespace
	- Queue/topic name: target entity
	- Authentication method: managed identity or connection string
6. Service Bus namespace > target queue/topic > test message path
	- Send test message
	- Consume from target service
	- Check retry behavior
	- Check dead-letter behavior

**Reference:**
- Link: Azure Service Bus overview: https://learn.microsoft.com/azure/service-bus-messaging/service-bus-messaging-overview
- Best practice: Azure Service Bus performance best practices: https://learn.microsoft.com/azure/service-bus-messaging/service-bus-performance-improvements
- Diagram: Azure messaging services comparison: https://learn.microsoft.com/azure/service-bus-messaging/compare-messaging-services

## Microsoft Commercial Marketplace / Partner Center

Microsoft Commercial Marketplace / Partner Center is needed for the future transactable offer publication path. The required function is offer setup and technical configuration for SaaS and/or Azure Application / Managed Application, and the meeting-specific need is to continue publication readiness discussion in ME-127200.

| Offer type | Meeting-specific item | Decision item |
|---|---|---|
| SaaS offer | Cloud-hosted dashboard / web platform | Confirm whether the Azure-hosted platform should be SaaS |
| Azure Application / Managed Application | Hybrid deployment model | Confirm whether customer-side Azure resource deployment is required |
| Pricing | Low-priced public base plan and enterprise private offers | Confirm pricing model and private offer structure |

### 1. SaaS offer
1. Partner Center > `Marketplace offers` > `New offer` > `Software as a Service`
	- Offer ID: target offer ID
	- Offer alias: target offer alias
2. `Create`
3. SaaS offer > `Offer setup`
	- Setup values: target offer setup values
4. SaaS offer > `Properties`
	- Categories and legal/support values: target values
5. SaaS offer > `Offer listing`
	- Listing text and assets: approved values
6. SaaS offer > `Preview audience`
	- Preview account: test account
7. SaaS offer > `Plan overview`
	- Plan ID: target plan ID
	- Plan name: target plan name
8. SaaS offer > `Technical configuration`
	- Landing page URL: ELISA landing page URL
	- Connection webhook URL: ELISA webhook URL
	- Microsoft Entra tenant ID: target tenant ID
	- Microsoft Entra application ID: target application ID
9. `Review and publish`
	- Target: preview
	- Check: purchase, landing page, activation, webhook flow

### 2. Azure Application / Managed Application offer
1. Partner Center > `Marketplace offers` > `New offer` > `Azure Application`
	- Offer ID: target offer ID
	- Offer alias: target offer alias
2. `Create`
3. Azure Application offer > `Offer setup`
	- Setup values: target offer setup values
4. Azure Application offer > `Properties`
	- Categories and legal/support values: target values
5. Azure Application offer > `Offer listing`
	- Listing text and assets: approved values
6. Azure Application offer > `Plan overview` > `Create new plan`
	- Plan type: `Managed Application`, if publisher-managed customer-side resources are required
7. Plan > deployment package
	- ARM template package: upload target package
	- Parameters file: upload target parameters
8. Plan > managed application settings
	- Managed resource group: target setting
	- Publisher authorization: target Microsoft Entra object ID and role
9. `Review and publish`
	- Target: preview
	- Check: deployment into test subscription

### 3. Pricing and private offer structure
1. Offer plan > `Pricing and availability`
	- Public plan: at least one plan
	- Pricing model: flat rate, per user, or custom meter
2. Metered pricing, if required
	- Dimension: camera stream count or usage time
	- Unit: target unit
	- Price: target price
3. Partner Center > `Private offers` > `Create private offer`
	- Customer billing account or tenant: target customer value
	- Private price: target private offer price
	- Start date: target date
	- End date: target date
4. Private offer > `Review and submit`
	- Check public plan pricing
	- Check private offer pricing

**Reference:**
- Link: SaaS offer creation in Partner Center: https://learn.microsoft.com/partner-center/marketplace/create-new-saas-offer
- Best practice: SaaS fulfillment API v2: https://learn.microsoft.com/partner-center/marketplace/partner-center-portal/pc-saas-fulfillment-api-v2
- Diagram: Azure Application offer planning: https://learn.microsoft.com/partner-center/marketplace/plan-azure-app-offer
- Link: Azure Managed Application planning: https://learn.microsoft.com/partner-center/marketplace/plan-azure-app-managed-app
- Link: Private offers in Partner Center: https://learn.microsoft.com/partner-center/marketplace/private-offers

## Next checkpoints

1. Review the Redis usage pattern and decide whether managed Azure Cache for Redis or self-managed Redis-compatible deployment should be evaluated first.
2. Confirm the tenant boundary for future Microsoft Entra ID / multitenant design.
3. Confirm the first Azure migration workload scope for frontend, backend, media storage, serverless jobs, database, queues, and cache.
4. Continue offer type and Partner Center publication readiness discussion in ME-127200 during the week of 18/05-22/05.

Best regards,

Kim
v-kimpy@microsoft.com
