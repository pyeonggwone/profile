2. DOCX / DOC
1순위: C# (.NET) + Open XML SDK 또는 Word Interop
이유:

Open XML SDK 는 Microsoft 공식, OOXML 의 모든 element 를 strongly-typed 로 노출. Linux/macOS 에서도 dotnet 으로 동작.
Word COM Interop 은 .doc (legacy binary format) 까지 처리 가능.
DocumentFormat.OpenXml NuGet 패키지가 사실상 표준.