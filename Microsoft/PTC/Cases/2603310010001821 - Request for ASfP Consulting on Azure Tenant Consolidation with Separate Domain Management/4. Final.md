안녕하세요, 이용덕(Aiden Lee)님.

문의 주신 내용은 “여러 계열사의 Azure / Microsoft Entra tenant를 하나로 통합하면서도 회사별 email 및 identity domain을 분리 운영할 수 있는가”로 이해했습니다. 아래는 고객 문의 원문에서 확인되는 질문별 답변에, 실제 포털에서 어떤 항목을 구성하면 되는지 연결한 내용입니다.

> 결론: 단일 Microsoft Entra tenant 안에서 여러 custom domain을 등록하고 회사별 UPN / email domain을 운영하는 구성은 가능합니다. 다만 회사별로 완전히 독립된 tenant 수준의 격리를 제공하는 구조는 아니므로, domain 이전, policy scope, 관리자 위임, 기존 tenant 의존성은 별도 설계 항목으로 다뤄야 합니다.

## 질문별 답변 요약

| 고객 질문 | 직접 답변 |
|---|---|
| 여러 계열사의 Azure tenants를 단일 tenant로 통합할 수 있는가? | 가능합니다. 통합 후 identity / access management의 기본 경계는 단일 Microsoft Entra tenant가 됩니다. 다만 기존 tenant가 있는 계열사는 전환 기간 동안 target tenant와 source tenant 양쪽의 cross-tenant 설정이 필요할 수 있습니다. |
| 단일 tenant 안에서 회사별 email 및 identity domain을 분리 운영할 수 있는가? | 가능합니다. 여러 custom domain을 하나의 tenant에 추가하고 사용자 UPN / email suffix로 회사별 domain을 사용할 수 있습니다. |
| 여러 회사 domain을 unified tenant에서 어떻게 관리하는가? | 회사별 domain을 custom domain으로 추가하고 DNS ownership을 확인한 뒤, 사용자, group, attribute, naming standard로 운영 단위를 구분합니다. 기존 tenant 공존 구간은 Cross-tenant access settings와 Cross-tenant synchronization으로 별도 관리합니다. |
| multi-entity 환경에 적합한 identity, authentication, governance 모델은 무엇인가? | 중앙 tenant + 회사별 Administrative unit / security group / scoped role / Conditional Access scope 조합이 현실적인 기본 모델입니다. 기존 tenant 사용자는 source tenant outbound와 target tenant inbound 설정을 함께 검증해야 합니다. |
| 주요 제한, 위험, 운영 고려사항은 무엇인가? | custom domain 중복, tenant-level policy 영향, 완전한 관리자 격리 한계, 기존 tenant의 subscription / app / mail flow 의존성이 핵심 위험입니다. |
| Microsoft best practice에 맞는 enterprise group 구조는 무엇인가? | Microsoft Entra tenant 설계와 Azure landing zone의 management group / subscription 구조를 분리해 설계하는 것이 적절합니다. |

## 1. 단일 tenant 통합 가능 여부

**답변: 가능합니다.** 여러 계열사의 identity를 하나의 Microsoft Entra tenant로 통합하는 target model은 검토할 수 있습니다. 이 경우 단일 tenant가 identity, authentication, access policy, directory role의 공통 운영 경계가 됩니다.

권장 설계는 그룹 공통 Microsoft Entra tenant를 target tenant로 정의하고, 계열사 구분은 company attribute, group, Administrative unit, naming standard로 처리하는 방식입니다. 관리자 모델은 중앙 Global Administrator / Security Administrator와 회사별 scoped administrator를 분리하고, 정책 모델은 공통 security baseline과 회사별 Conditional Access scope를 조합하는 형태가 적절합니다.

기존 Azure / Microsoft Entra tenant를 이미 운영 중인 계열사는 한 번에 모든 identity를 이전하기보다, source tenant와 target tenant가 공존하는 pilot 기간을 두는 것이 현실적입니다. 이때 target tenant에서는 source tenant의 inbound B2B collaboration과 inbound cross-tenant synchronization을 허용하고, source tenant에서는 target tenant로의 outbound B2B collaboration과 synchronization configuration을 구성해야 합니다.

**구현 방법**

- Microsoft Entra admin center > Identity > Overview
- Target tenant baseline
	- Tenant name: `Korea Enterprise Group Unified Tenant`
	- Tenant ID: `8f3c2a9e-5d41-4c7a-9b28-6e1f0d3a72c4`
	- Primary domain: `kegroup.co.kr`
- Existing affiliated tenant baseline
	- Tenant name: `Korea Enterprise Manufacturing`
	- Tenant ID: `4c9a7d20-6f82-45e1-9c34-0b7e6d219f5a`
	- Primary domain: `keg-manufacturing.co.kr`
- Existing affiliated tenant coexistence
	- Source tenant outbound access: target tenant 대상으로 `B2B collaboration` 허용
	- Target tenant inbound access: source tenant 대상으로 `B2B collaboration` 허용
	- Target tenant inbound sync: source tenant의 `Cross-tenant sync` 허용
- New Azure adoption entity baseline
	- Entity name: `Korea Enterprise Logistics`
	- Planned domain: `keg-logistics.co.kr`
	- Identity source: `Cloud-only users`

**주의할 점:** 단일 tenant 통합은 운영 표준화에는 유리하지만, 계열사별 독립 tenant boundary를 유지하는 것과는 다릅니다. Microsoft 문서에서도 tenant는 identity and access management의 운영 범위로 설명됩니다. 계열사별 규제, audit ownership, data residency, 보안 책임 분리가 tenant 단위로 요구된다면 단일 tenant 통합 대신 multi-tenant 유지 또는 phased coexistence도 선택지로 남겨야 합니다.

Reference: [Multitenant organization capabilities in Microsoft Entra ID](https://learn.microsoft.com/en-us/entra/identity/multi-tenant-organizations/overview)

## 2. 회사별 email 및 identity domain 분리 운영

**답변: 가능합니다.** 단일 Microsoft Entra tenant에 여러 custom domain을 추가하고, 사용자별 UPN suffix 또는 email domain으로 각 회사 domain을 사용할 수 있습니다.

Microsoft Learn은 custom domain에 대해 “you can add your organization's DNS name as a custom domain name and set it as primary”라고 설명합니다. 이 구조를 사용하면 하나의 tenant 안에서도 `keg-manufacturing.co.kr`, `keg-logistics.co.kr`, `keg-retail.co.kr` 같은 회사별 identity domain을 운영할 수 있습니다.

다만 기존 tenant에서 이미 사용 중인 domain은 target tenant에 동시에 verify할 수 없습니다. 따라서 기존 tenant 사용자와 target tenant 사용자가 공존하는 기간에는 custom domain 이전과 cross-tenant collaboration을 분리해서 설계해야 합니다. Cross-tenant synchronization은 기존 tenant의 사용자를 target tenant에 B2B collaboration user로 동기화할 수 있지만, custom domain ownership 이전 자체를 대신하지는 않습니다.

**구현 방법**

- Microsoft Entra admin center > Identity > Settings > Domain names
- Add custom domain
	- Custom domain name: `keg-manufacturing.co.kr`
- Add custom domain
	- Custom domain name: `keg-logistics.co.kr`
- Add custom domain
	- Custom domain name: `keg-retail.co.kr`
- `keg-manufacturing.co.kr` > DNS record values
	- Record type: `TXT`
	- Alias or host name: `@`
	- Destination or points to address: `MS=ms47283916`
- Verify

- Microsoft 365 admin center > Settings > Domains
- Add domain
	- Domain name: `keg-manufacturing.co.kr`
- Add domain
	- Domain name: `keg-logistics.co.kr`
- `keg-manufacturing.co.kr` > DNS records
	- MX: `keg-manufacturing-co-kr.mail.protection.outlook.com`
	- TXT: `v=spf1 include:spf.protection.outlook.com -all`
	- CNAME: `autodiscover.outlook.com`

**중요 제약:** 동일한 custom domain은 동시에 여러 tenant에서 verify할 수 없습니다. Microsoft Learn은 “A domain name can only be verified in one directory”라고 명시합니다. 따라서 기존 계열사 tenant에서 사용 중인 domain을 target tenant에 등록하려면 domain 제거 / 이전 순서, 사용자 로그인 영향, Exchange mail flow 영향, 관련 workload 영향을 먼저 설계해야 합니다.

기존 tenant 공존이 필요한 경우에는 domain 이전 전까지 source tenant의 사용자 UPN / mail domain은 기존 tenant에 유지하고, target tenant에는 B2B collaboration user 또는 cross-tenant synchronized user 형태로 접근을 검증합니다. 이후 domain cutover 시점에 custom domain 제거 / 추가, 사용자 UPN 변경, Exchange Online accepted domain 및 mail flow 변경을 별도 migration 절차로 처리해야 합니다.

Reference: [Add your custom domain name to your tenant](https://learn.microsoft.com/en-us/entra/fundamentals/add-custom-domain)

## 3. unified tenant에서 여러 회사 domain 관리 방식

**답변: company domain은 custom domain으로 관리하고, 운영 경계는 identity object와 governance object로 나누어 관리하는 방식이 적절합니다.**

domain ownership은 회사별 custom domain의 DNS TXT 또는 MX record로 확인하고, user identity는 회사별 UPN suffix, company attribute, department attribute로 표준화합니다. group은 회사별 user group, admin group, application access group으로 분리하고, role delegation은 Administrative unit과 scoped role assignment를 활용합니다. Conditional Access 정책은 회사별 group 또는 admin group 기준으로 scope를 지정합니다.

기존 tenant가 있는 회사는 unified tenant 내부 객체 관리와 cross-tenant 공존 관리를 구분해야 합니다. Unified tenant 내부에서는 users / groups / Administrative units / Conditional Access로 운영 단위를 나누고, source tenant와 target tenant 사이에서는 Cross-tenant access settings의 inbound / outbound scope와 Cross-tenant synchronization scope를 별도로 제한합니다.

**구현 방법**

- Microsoft Entra admin center > Identity > Users > All users
- New user > Create new user
	- User principal name: `minjun.kim@keg-manufacturing.co.kr`
	- Display name: `Minjun Kim`
	- Company name: `Korea Enterprise Manufacturing`
	- Department: `Plant Operations`
- New user > Create new user
	- User principal name: `seoyeon.park@keg-logistics.co.kr`
	- Display name: `Seoyeon Park`
	- Company name: `Korea Enterprise Logistics`
	- Department: `Logistics Operations`
- Microsoft Entra admin center > Identity > Groups > All groups
- New group
	- Group type: `Security`
	- Group name: `KEG Manufacturing Users`
	- Membership type: `Assigned`
	- Members: `minjun.kim@keg-manufacturing.co.kr`
- New group
	- Group type: `Security`
	- Group name: `KEG Logistics Users`
	- Membership type: `Assigned`
	- Members: `seoyeon.park@keg-logistics.co.kr`
- Microsoft Entra admin center > Identity > External Identities > Cross-tenant access settings > Organizational settings
- `Korea Enterprise Manufacturing` > Inbound access > B2B collaboration
	- Users and groups: `Allow access`
	- Applies to: `Select users and groups`
	- External users and groups: `KEG Manufacturing Migration Pilot Users`
- Source tenant > Outbound access > B2B collaboration
	- Target tenant: `Korea Enterprise Group Unified Tenant`
	- Users and groups: pilot 사용자 group
	- Applications: pilot 대상 application

이 방식은 회사별 운영을 “구분”하는 모델이지, 회사별 tenant를 “분리”하는 모델은 아닙니다. 완전한 보안 경계가 필요하면 단일 tenant 통합 대신 multi-tenant coexistence도 같이 검토해야 합니다.

## 4. identity, authentication, governance 모델

**답변: 중앙 통제 + 회사별 위임 운영 모델을 권장합니다.** 단일 tenant 사용자 객체와 회사별 UPN suffix / attribute를 조합하면 사용자 표준화와 domain 분리 운영을 동시에 달성할 수 있습니다. Authentication은 공통 baseline과 회사별 Conditional Access 조건을 조합하고, Governance는 Administrative unit, scoped role, security group으로 구성하는 방식이 현실적입니다.

기존 tenant 공존 구간의 identity 모델은 target tenant를 최종 운영 기준으로 두되, source tenant 사용자를 B2B collaboration 또는 Cross-tenant synchronization 대상으로 제한적으로 받아들이는 방식이 적절합니다. Authentication과 access control은 target tenant의 Conditional Access만 보는 것이 아니라 source tenant outbound access, target tenant inbound access, tenant restrictions, MFA trust 여부를 함께 검토해야 합니다.

Administrative unit에 대해서 Microsoft Learn은 “Administrative units restrict permissions in a role to any portion of your organization that you define”라고 설명합니다. 따라서 회사별 User Administrator 또는 Group Administrator 위임에는 적합합니다. 다만 같은 문서에서 “Administrative units apply scope only to management permissions”라고 설명하므로, Administrative unit을 tenant isolation처럼 해석하면 안 됩니다.

**구현 방법**

- Microsoft Entra admin center > Identity > Roles & admins > Admin units
- Add
	- Name: `AU-KEG-Manufacturing`
	- Description: `Administrative unit for Korea Enterprise Manufacturing identities`
- Add
	- Name: `AU-KEG-Logistics`
	- Description: `Administrative unit for Korea Enterprise Logistics identities`
- `AU-KEG-Manufacturing` > Users > Add members
	- Members: `minjun.kim@keg-manufacturing.co.kr`
- `AU-KEG-Manufacturing` > Groups > Add members
	- Members: `KEG Manufacturing Users`
- `AU-KEG-Manufacturing` > Roles and administrators
- Add assignments
	- Role: `User Administrator`
	- Select member: `identity-admin-mfg@kegroup.co.kr`
- Microsoft Entra admin center > Identity > Roles & admins > Roles & admins
- `Global Administrator` > Assignments
	- Member: `tenant-platform-admin@kegroup.co.kr`
- `Security Administrator` > Assignments
	- Member: `security-admin@kegroup.co.kr`
- Microsoft Entra admin center > Protection > Conditional Access > Policies
- New policy
	- Name: `CA-KEG-Manufacturing-Admins-Require-MFA`
	- Users: `KEG Manufacturing Admins`
	- Target resources: `All cloud apps`
	- Grant: `Require multifactor authentication`
	- Enable policy: `Report-only`
- Target tenant > Identity > External Identities > Cross-tenant access settings > Organizational settings
- `Korea Enterprise Manufacturing` > Inbound access
	- B2B collaboration: pilot users / pilot applications만 허용
	- Cross-tenant sync: `Allow users sync into this tenant` 사용 여부 결정
- Source tenant > Identity > External Identities > Cross-tenant synchronization
- Configurations > New configuration
	- Tenant ID: target tenant ID
	- Scope: pilot 사용자 또는 pilot group
	- Provision on demand 후 Start provisioning

Reference: [Administrative units in Microsoft Entra ID](https://learn.microsoft.com/en-us/entra/identity/role-based-access-control/administrative-units), [What is Conditional Access?](https://learn.microsoft.com/en-us/entra/identity/conditional-access/overview)

## 5. 제한 사항, 위험, 운영 고려사항

**답변: 가장 큰 위험은 domain 이전, tenant-level policy 영향, 완전한 관리자 격리 한계, 기존 tenant 의존성입니다.**

Custom domain은 한 directory에서만 verify 가능하므로, 기존 tenant에 남아 있는 domain은 target tenant에 바로 추가할 수 없습니다. 이 항목은 Microsoft Entra admin center > Identity > Settings > Domain names에서 회사별 domain 상태를 확인하고, Microsoft 365가 포함된 경우 Microsoft 365 admin center > Settings > Domains의 email domain 상태와 함께 봐야 합니다.

Tenant-level policy는 Conditional Access > Policies에서 대상 users / groups / cloud apps가 과도하게 넓지 않은지 살펴야 합니다. 중앙 정책이 전체 tenant에 영향을 줄 수 있으므로, 회사별 group을 target으로 두고 초기에는 `Report-only`로 운영하는 접근이 안전합니다.

관리자 분리 한계는 Administrative unit과 Roles & admins를 함께 봐야 합니다. 회사별 admin이 scoped role인지, tenant-wide role인지가 핵심입니다. Administrative unit은 관리 권한 scope를 제한하지만 데이터, 감사, 정책, 보안 경계를 tenant처럼 완전히 분리하지는 않습니다.

기존 tenant 의존성은 tenant overview, enterprise applications, subscriptions, Microsoft 365 domains, Conditional Access policies에서 함께 정리해야 합니다. 기존 app registration, enterprise application, SSO, mail flow, subscription ownership이 남아 있다면 tenant consolidation은 identity 변경만으로 끝나지 않습니다.

Cross-tenant synchronization은 여러 tenant 간 B2B collaboration user 생성 / 업데이트 / 삭제를 자동화하는 기능입니다. 다만 Microsoft Learn FAQ는 “Cross-tenant synchronization isn't a migration tool”이라고 명시합니다. 따라서 기존 tenant가 있는 계열사는 cross-tenant synchronization을 tenant migration 대체 수단이 아니라, 단계적 공존 또는 협업 개선 수단으로 보는 것이 맞습니다.

운영 점검 항목:

- Microsoft Entra admin center > Identity > External Identities > Cross-tenant access settings
- Organizational settings
- Inbound / outbound collaboration scope

**기존 tenant 공존을 위한 cross-tenant 설정 방법**

Cross-tenant access / synchronization은 반드시 target tenant와 source tenant 양쪽에서 설정해야 합니다. Target tenant에서는 inbound access와 inbound synchronization을 허용하고, source tenant에서는 outbound access와 synchronization configuration을 구성합니다. 또한 Cross-tenant 설정은 크게 두 가지를 구분해야 합니다. 첫째는 tenant 간 B2B collaboration 허용 범위를 정하는 Cross-tenant access settings이고, 둘째는 기존 tenant의 사용자를 target tenant에 B2B collaboration user로 자동 프로비전하는 Cross-tenant synchronization입니다.

1. Target tenant에서 기존 계열사 tenant를 조직으로 추가합니다.
	- Microsoft Entra admin center > Identity > External Identities > Cross-tenant access settings > Organizational settings
	- Add organization
	- Search by domain name or tenant ID: `4c9a7d20-6f82-45e1-9c34-0b7e6d219f5a`
	- Add
2. Target tenant의 inbound access를 제한적으로 허용합니다.
	- `Korea Enterprise Manufacturing` > Inbound access > B2B collaboration
	- Users and groups: `Allow access`
	- Applies to: `Select users and groups`
	- External users and groups: `KEG Manufacturing Migration Pilot Users`
	- Applications: `Allow access`
	- Applies to: `Select applications`
	- Applications: `Microsoft Office 365` 또는 pilot 대상 application
	- Save
3. Source tenant의 outbound access를 target tenant 대상으로 허용합니다.
	- Source tenant의 Microsoft Entra admin center > Identity > External Identities > Cross-tenant access settings > Organizational settings
	- Target tenant 추가
	- Outbound access > B2B collaboration
	- Users and groups: `Allow access`
	- Applies to: `Select users and groups`
	- Users and groups: pilot 사용자 group
	- Applications: pilot 대상 application
	- Save
4. Cross-tenant synchronization을 사용할 경우 target tenant에서 inbound synchronization을 허용합니다.
	- Target tenant > Cross-tenant access settings > Organizational settings
	- Source tenant 선택
	- Inbound access > Cross-tenant sync
	- Allow users sync into this tenant: `On`
	- Save
5. Source tenant에서 synchronization configuration을 만듭니다.
	- Source tenant > Identity > External Identities > Cross-tenant synchronization
	- Configurations > New configuration
	- Tenant ID: target tenant ID
	- Scope: pilot 사용자 또는 pilot group
	- Attribute mappings: 기본 mapping 검토 후 필요한 `company`, `department`, `userPrincipalName`, `mail` 속성 확인
	- Test connection
	- Provision on demand로 pilot 사용자 1명을 먼저 검증
	- Start provisioning

이 설정은 기존 tenant와 target tenant가 일정 기간 공존하면서 application access, Teams / SharePoint 협업, 단계적 사용자 전환을 검증할 때 유용합니다. 하지만 custom domain 이전, mailbox migration, subscription 이동, application registration 이전을 자동으로 처리하지는 않으므로 tenant consolidation migration plan과 별도 workstream으로 관리해야 합니다.

Reference: [What is cross-tenant synchronization?](https://learn.microsoft.com/en-us/entra/identity/multi-tenant-organizations/cross-tenant-synchronization-overview)

## 6. Microsoft best practice 관점의 enterprise group 구조

**답변: Microsoft Entra tenant 구조와 Azure subscription / management group 구조를 분리해서 설계하는 것이 적절합니다.**

단일 tenant 여부는 identity boundary 결정이고, enterprise group의 Azure 운영 구조는 management group, subscription, landing zone, Azure Policy, RBAC 설계로 결정됩니다. 두 결정을 하나로 묶으면 안 됩니다.

Microsoft Entra tenant는 그룹 공통 identity plane으로 사용할지 결정하는 영역입니다. 회사별 identity boundary는 UPN suffix, group, Administrative unit으로 표현합니다. Authentication baseline은 공통 baseline과 회사별 exception을 조합하고, email domain boundary는 Microsoft 365 admin center의 Domains와 DNS records 설계로 연결됩니다. 기존 tenant 공존이 필요하면 Cross-tenant access settings에서 collaboration scope를 별도로 설계합니다.

Azure landing zone 문서는 reference architecture를 “opinionated target architecture”라고 설명하며, design areas에 Microsoft Entra tenant, identity and access management, management group and subscription organization, security, governance가 포함됩니다. 따라서 고객 시나리오에서는 tenant 통합 여부와 함께 Azure landing zone 기준의 subscription / management group 설계를 병행하는 것이 Microsoft best practice에 더 가깝습니다.

Reference: [What is an Azure landing zone?](https://learn.microsoft.com/en-us/azure/cloud-adoption-framework/ready/landing-zone/)

참고 아키텍처:

- Azure landing zone hub-spoke reference architecture: [Azure landing zone architecture diagram](https://learn.microsoft.com/en-us/azure/cloud-adoption-framework/ready/enterprise-scale/media/azure-landing-zone-architecture-diagram-hub-spoke.svg#lightbox)
- Platform landing zone / application landing zone 구조: [Azure landing zone reference architecture with Application Landing Zones & Platform Landing Zone](https://learn.microsoft.com/en-us/azure/cloud-adoption-framework/ready/landing-zone/media/alz-application-platform.svg#lightbox)
- Administrative unit 위임 개념: [Administrative unit overview diagram](https://learn.microsoft.com/en-us/entra/identity/role-based-access-control/media/administrative-units/admin-unit-overview.png#lightbox)
- Cross-tenant synchronization 개념: [Cross-tenant synchronization diagram](https://learn.microsoft.com/en-us/entra/identity/multi-tenant-organizations/media/cross-tenant-synchronization-overview/cross-tenant-synchronization-diagram.png#lightbox)

## 7. 한국어 기반 심화 논의

**답변: 본 건은 Microsoft Entra ID tenant consolidation, multi-domain identity design, governance model 중심의 한국어 설계 논의로 진행하는 것이 적절합니다.** 후속 논의에서는 아래 산출물을 기준으로 한국어로 architecture review를 진행하면 됩니다.

- Target tenant model: full consolidation / phased coexistence / long-term multi-tenant 중 선택
- Domain transition: 회사별 custom domain 이전 순서 및 영향도
- Delegated administration: 회사별 Administrative unit, role assignment, admin group 설계
- Authentication policy: 회사별 Conditional Access baseline 및 exception 설계
- Azure organization: management group, subscription, landing zone 정렬

## 권장 pilot 흐름

1. Target tenant와 기존 tenant의 tenant name, tenant ID, primary domain, verified domain을 정리합니다.
2. 회사별 custom domain을 target tenant에 추가하고 DNS ownership record를 준비합니다.
3. 회사별 test user를 만들고 UPN suffix, company attribute, department, usage location을 표준화합니다.
4. 회사별 user group, admin group을 만들고 application access와 policy target의 기준으로 사용합니다.
5. 회사별 Administrative unit을 만들고 users / groups를 배치한 뒤 scoped role을 할당합니다.
6. Conditional Access 정책은 회사별 group 대상으로 만들고 초기에는 `Report-only`로 운영합니다.
7. 기존 tenant가 있는 계열사는 Cross-tenant access settings와 Cross-tenant synchronization을 pilot 범위로 구성해 공존 기간의 collaboration access를 검증합니다.
8. Microsoft 365 email domain과 기존 tenant coexistence 항목은 mail flow와 collaboration 영향이 보이는 범위에서 별도 일정으로 다룹니다.

## 설계 확정을 위해 필요한 입력값

아래 값은 답변을 회피하기 위한 전제가 아니라, 위 답변을 실제 고객 환경에 맞는 target architecture로 확정하기 위해 필요한 입력입니다.

- 계열사별 현재 Azure / Microsoft Entra tenant 수, tenant ID, primary domain, verified domain
- 계열사별 subscription 수, billing owner, management group 사용 여부
- 계열사별 identity source: cloud-only, Active Directory, Microsoft Entra Connect, Cloud Sync, federation
- Exchange Online, Teams, SharePoint, OneDrive, Power BI 포함 여부
- app registration, enterprise application, SSO, Conditional Access dependency
- 회사별 audit, compliance, data residency, privileged admin separation 요구

## Sample value

아래 값은 포털 구현 방향을 설명하기 위한 예시입니다. 실제 문서에서는 고객 환경 값으로 대체하면 됩니다.

- Target tenant name: `Korea Enterprise Group Unified Tenant`
- Target tenant ID: `8f3c2a9e-5d41-4c7a-9b28-6e1f0d3a72c4`
- Target primary domain: `kegroup.co.kr`
- Existing affiliate tenant name: `Korea Enterprise Manufacturing`
- Existing affiliate tenant ID: `4c9a7d20-6f82-45e1-9c34-0b7e6d219f5a`
- Manufacturing domain: `keg-manufacturing.co.kr`
- Logistics domain: `keg-logistics.co.kr`
- Retail domain: `keg-retail.co.kr`
- Manufacturing admin unit: `AU-KEG-Manufacturing`
- Logistics admin unit: `AU-KEG-Logistics`
- Manufacturing users group: `KEG Manufacturing Users`
- Logistics users group: `KEG Logistics Users`
- Manufacturing admins group: `KEG Manufacturing Admins`
- Tenant platform admin: `tenant-platform-admin@kegroup.co.kr`
- Security admin: `security-admin@kegroup.co.kr`
- Conditional Access admin: `ca-admin@kegroup.co.kr`

## 공식 참고 문서

- Custom domain 추가 및 domain 중복 제약: [Add your custom domain name to your tenant](https://learn.microsoft.com/en-us/entra/fundamentals/add-custom-domain)
- Administrative unit 기반 운영 위임: [Administrative units in Microsoft Entra ID](https://learn.microsoft.com/en-us/entra/identity/role-based-access-control/administrative-units)
- Conditional Access 정책 모델: [What is Conditional Access?](https://learn.microsoft.com/en-us/entra/identity/conditional-access/overview)
- Multi-tenant organization 개념: [Multitenant organization capabilities in Microsoft Entra ID](https://learn.microsoft.com/en-us/entra/identity/multi-tenant-organizations/overview)
- Cross-tenant synchronization: [What is cross-tenant synchronization?](https://learn.microsoft.com/en-us/entra/identity/multi-tenant-organizations/cross-tenant-synchronization-overview)
- Azure landing zone reference architecture: [What is an Azure landing zone?](https://learn.microsoft.com/en-us/azure/cloud-adoption-framework/ready/landing-zone/)